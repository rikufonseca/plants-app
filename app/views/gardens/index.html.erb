<h1> All the gardens </h1>

<ul>
  <% @gardens.each do |garden| %>
    <li> <%= link_to garden.name, garden_path(garden) %> </li>
    <li> <%= image_tag(garden.banner_url) %></li>
  <% end %>
</ul>


